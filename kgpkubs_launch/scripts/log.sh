#!/bin/bash
# Script to log the data from the BeliefState topic to analyze

DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )"
source $DIR/functions.sh

# TODO : Add the necessary files to start logging